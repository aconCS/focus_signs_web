/**
 * TODO: replace with the real production domain once the client confirms it,
 * then set NEXT_PUBLIC_SITE_URL in the hosting environment to match.
 * Everything SEO-related (sitemap, robots, canonicals, JSON-LD) reads from here.
 */
export const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.focussigns.cy";
